// READ: Input data is in the test file

export const generateTimeSlots = shift => {
  // TODO: fill in this function to generate timeslots for the shift
  const timeslots = [];
  return timeslots;
};

export const calculateAvailability = (timeslots, reservations) => {
  /*
  TODO: Calculate the availability of the shift times using the reservations.

  We want to know for each time if there is a table available.
  */
  return timeslots;
};